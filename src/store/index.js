import { createStore } from "vuex";

export default createStore({
  strict: false,
  state: {
    deliveryInfo : {
      APIKEY : '0mMEv1tRdOGpva8oiwnTjg' ,
    }
  },
  modules: {
    // todoStore: todoStore,
  },
});