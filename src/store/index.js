import { createStore } from "vuex";

export default createStore({
  strict: false,
  state: {
    deliveryInfo : {
      APIKEY : 'Uv1Q2h9xNr8zmnahxAAaFA' ,
    }
  },
  modules: {
    // todoStore: todoStore,
  },
});